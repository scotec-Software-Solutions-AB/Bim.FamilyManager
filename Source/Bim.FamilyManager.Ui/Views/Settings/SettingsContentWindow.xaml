<Window x:Class="Bim.FamilyManager.Ui.Views.Settings.SettingsContentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:resources="clr-namespace:Bim.FamilyManager.Ui.Resources"
        xmlns:settings="clr-namespace:Bim.FamilyManager.Ui.ViewModels.Settings"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner"
        SizeToContent="Manual"
        Title="{x:Static resources:StringResources.FamilySourceSettings_Window_Title}" Height="450" Width="800"
        d:DataContext="{d:DesignInstance settings:FamilySourceSettingsEditViewModel, IsDesignTimeCreatable=false}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary
                    Source="pack://application:,,,/Bim.FamilyManager.Ui;component/Themes/Generic.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>


    <Grid Background="{StaticResource SecondaryBackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ContentPresenter Grid.Row="0" Content="{Binding}" ContentTemplateSelector="{Binding DataTemplateSelector}"
                          Margin="10,10,10,10" />

    </Grid>
</Window>