<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:wpf="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../Colors.xaml" />
        <ResourceDictionary Source="../Icons.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="ToggleButton" x:Key="ExpandButtonStyle">
        <Setter Property="Margin" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Background" Value="{StaticResource BackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Grid>
                        <wpf:MaterialIcon x:Name="ToggleImage" Height="20" Background="{TemplateBinding Background}"
                                          Kind="{StaticResource IconExpand}"
                                          Foreground="{StaticResource ButtonLoadBrush}"
                                          RenderTransformOrigin="0.5,0.5">
                            <wpf:MaterialIcon.RenderTransform>
                                <RotateTransform Angle="0" />
                            </wpf:MaterialIcon.RenderTransform>
                        </wpf:MaterialIcon>


                    </Grid>
                    <ControlTemplate.Triggers>
                        <!-- Trigger for IsChecked to rotate the image -->
                        <Trigger Property="IsChecked" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="ToggleImage"
                                                         Storyboard.TargetProperty="RenderTransform.(RotateTransform.Angle)"
                                                         To="90"
                                                         Duration="0:0:0.2" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="ToggleImage"
                                                         Storyboard.TargetProperty="RenderTransform.(RotateTransform.Angle)"
                                                         To="0"
                                                         Duration="0:0:0.2" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <!-- Trigger for MouseOver to change the image -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ToggleImage" Property="Opacity"
                                    Value="1.0" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="False">
                            <Setter TargetName="ToggleImage" Property="Opacity"
                                    Value="0.75" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


</ResourceDictionary>