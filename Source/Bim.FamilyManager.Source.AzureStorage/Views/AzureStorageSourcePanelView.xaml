<UserControl x:Class="Bim.FamilyManager.Source.AzureStorage.Views.AzureStorageSourcePanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:resources="clr-namespace:Bim.FamilyManager.Source.AzureStorage.Resources"
             xmlns:controls1="clr-namespace:Bim.FamilyManager.Ui.Controls;assembly=Bim.FamilyManager.Ui"
             xmlns:viewModels="clr-namespace:Bim.FamilyManager.Source.AzureStorage.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="100" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance viewModels:AzureStorageSourcePanelViewModel, IsDesignTimeCreatable=false}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary
                    Source="pack://application:,,,/Bim.FamilyManager.Ui;component/Themes/Generic.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Border Background="{StaticResource SecondaryBackgroundBrush}"
                BorderThickness="0,0,0,1"
                BorderBrush="{StaticResource BorderBrush}">
            <StackPanel Orientation="Horizontal" Margin="5">
                <controls1:MaterialIconButton Kind="{StaticResource IconAccount}"
                                              IconSize="24" Padding="0" Margin="0"
                                              HorizontalContentAlignment="Center"
                                              HorizontalAlignment="Left"
                                              Command="{Binding SignInCommand}" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch" VerticalAlignment="Center">
                    <TextBlock Text="{x:Static resources:StringResources.AzureStorageSourceSettings_Text_SignedInAs}"
                               Margin="5,0,5,0" />
                    <TextBlock Text="{Binding SignedInAs}" Margin="5,0,0,0" />
                </StackPanel>
            </StackPanel>

        </Border>
    </Grid>
</UserControl>